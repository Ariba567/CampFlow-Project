import { useState } from 'react';
import { Download } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from '@/components/ui/dialog';
import { idOf, labelOf, type ApiItem } from '@/services/customerDashboardService';
export default function InvoiceDialog({ reservation }: { reservation: ApiItem }) { const [open, setOpen] = useState(false); const pricing = reservation.pricing ?? {}; return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button variant="outline" size="sm"><Download />Invoice</Button></DialogTrigger><DialogContent className="max-w-xl"><DialogHeader><DialogTitle className="font-serif text-3xl">Green Valley invoice</DialogTitle></DialogHeader><div className="space-y-5 rounded-xl border bg-card p-5 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Reservation</span><span className="font-semibold">#{reservation.reservationNumber ?? idOf(reservation).slice(-6)}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Campground</span><span>{labelOf(reservation.campground, 'Green Valley')}</span></div><div className="flex justify-between"><span className="text-muted-foreground">Stay</span><span>{String(reservation.checkIn ?? '').slice(0, 10)} — {String(reservation.checkOut ?? '').slice(0, 10)}</span></div><div className="border-t pt-4"><div className="flex justify-between"><span>Accommodation ({pricing.nights ?? 1} night{pricing.nights === 1 ? '' : 's'})</span><span>${Number(pricing.subtotal ?? pricing.total ?? 0).toFixed(2)}</span></div><div className="mt-2 flex justify-between"><span>Taxes & fees</span><span>${Number((pricing.taxes ?? 0) + (pricing.fees ?? 0)).toFixed(2)}</span></div><div className="mt-4 flex justify-between text-lg font-semibold"><span>Total</span><span>${Number(pricing.total ?? 0).toFixed(2)}</span></div></div></div><Button onClick={() => window.print()}><Download />Print / save as PDF</Button></DialogContent></Dialog>; }
