import { useEffect, useState } from 'react';
import { Eye, MapPinned, Pencil, Plus, Power } from 'lucide-react';
import { toast } from 'sonner';
import { Button } from '@/components/ui/button';
import { Card, CardContent } from '@/components/ui/card';
import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Spinner } from '@/components/ui/spinner';
import { Switch } from '@/components/ui/switch';
import { Textarea } from '@/components/ui/textarea';
import ErrorState from '@/components/ui/error-state';
import { DataTable, type DataColumn } from '@/components/operations/DataTable';
import { DataTableControls } from '@/components/operations/DataTableControls';
import { DataTablePagination } from '@/components/operations/DataTablePagination';
import { apiError, idOf, type ApiItem } from '@/services/operationsDashboardService';
import { createAdminCampground, getAdminCampground, listAdminCampgrounds, updateAdminCampground } from '@/services/adminManagementService';

type CampgroundForm = { id?: string; name: string; description: string; shortDescription: string; street: string; city: string; state: string; zip: string; country: string; phone: string; email: string; website: string; longitude: string; latitude: string; categories: string; amenities: string; totalSites: string; petPolicy: string; isActive: boolean; isFeatured: boolean };
const blankForm = (): CampgroundForm => ({ name: '', description: '', shortDescription: '', street: '', city: '', state: '', zip: '', country: 'US', phone: '', email: '', website: '', longitude: '', latitude: '', categories: 'tent', amenities: '', totalSites: '0', petPolicy: 'restricted', isActive: true, isFeatured: false });
const toForm = (item: ApiItem): CampgroundForm => ({ id: idOf(item), name: item.name ?? '', description: item.description ?? '', shortDescription: item.shortDescription ?? '', street: item.address?.street ?? '', city: item.address?.city ?? '', state: item.address?.state ?? '', zip: item.address?.zip ?? '', country: item.address?.country ?? 'US', phone: item.phone ?? '', email: item.email ?? '', website: item.website ?? '', longitude: String(item.location?.coordinates?.[0] ?? ''), latitude: String(item.location?.coordinates?.[1] ?? ''), categories: (item.categories ?? []).join(', '), amenities: (item.amenities ?? []).join(', '), totalSites: String(item.totalSites ?? 0), petPolicy: item.petPolicy ?? 'restricted', isActive: item.isActive !== false, isFeatured: Boolean(item.isFeatured) });
const csv = (value: string) => value.split(',').map((item) => item.trim()).filter(Boolean);

export default function DashboardAdminCampgrounds() {
  const [data, setData] = useState<{ rows: ApiItem[]; page: number; totalPages: number }>({ rows: [], page: 1, totalPages: 1 });
  const [search, setSearch] = useState(''); const [filter, setFilter] = useState('all'); const [sort, setSort] = useState('name'); const [order, setOrder] = useState<'asc' | 'desc'>('asc');
  const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [form, setForm] = useState<CampgroundForm | null>(null); const [details, setDetails] = useState<ApiItem | null>(null); const [saving, setSaving] = useState(false);
  const load = (page = data.page) => { setLoading(true); listAdminCampgrounds({ page, limit: 20, search: search || undefined, isActive: filter === 'all' ? undefined : filter === 'active', sort, order }).then((result) => setData({ rows: result.data, page: result.meta.page, totalPages: result.meta.totalPages })).catch((caught) => setError(apiError(caught, 'We could not load campgrounds.'))).finally(() => setLoading(false)); };
  useEffect(() => { const timer = window.setTimeout(() => load(1), 180); return () => window.clearTimeout(timer); }, [search, filter, sort, order]);
  const openDetails = async (item: ApiItem) => { try { setDetails(await getAdminCampground(idOf(item))); } catch (caught) { setError(apiError(caught, 'We could not load campground details.')); } };
  const save = async (event: React.FormEvent) => { event.preventDefault(); if (!form) return; const categories = csv(form.categories); const longitude = Number(form.longitude); const latitude = Number(form.latitude); if (form.description.trim().length < 10 || !form.name.trim() || !form.street.trim() || !form.city.trim() || !form.state.trim() || !form.zip.trim() || !form.phone.trim() || !form.email.trim() || !categories.length || !Number.isFinite(longitude) || !Number.isFinite(latitude) || Number(form.totalSites) < 0) { setError('Complete all required fields, including a description, category, valid coordinates, and site total.'); return; }
    const input = { name: form.name.trim(), description: form.description.trim(), shortDescription: form.shortDescription.trim() || undefined, location: { type: 'Point', coordinates: [longitude, latitude] }, address: { street: form.street.trim(), city: form.city.trim(), state: form.state.trim(), zip: form.zip.trim(), country: form.country.trim() || 'US' }, phone: form.phone.trim(), email: form.email.trim(), website: form.website.trim() || undefined, categories, amenities: csv(form.amenities), totalSites: Number(form.totalSites), petPolicy: form.petPolicy, isActive: form.isActive, isFeatured: form.isFeatured };
    setSaving(true); try { if (form.id) await updateAdminCampground(form.id, input); else await createAdminCampground(input); toast.success(form.id ? 'Campground updated' : 'Campground created'); setForm(null); load(); } catch (caught) { setError(apiError(caught, 'We could not save this campground.')); } finally { setSaving(false); } };
  const toggleActive = async (item: ApiItem) => { try { await updateAdminCampground(idOf(item), { isActive: item.isActive === false }); toast.success(item.isActive === false ? 'Campground reactivated' : 'Campground deactivated'); load(); } catch (caught) { setError(apiError(caught, 'We could not update campground status.')); } };
  const columns: DataColumn<ApiItem>[] = [
    { label: 'Campground', sortKey: 'name', cell: (row) => <div><p className="font-medium">{row.name}</p><p className="text-xs text-muted-foreground">{row.address?.city}, {row.address?.state}</p></div> },
    { label: 'Sites', sortKey: 'totalSites', cell: (row) => row.totalSites ?? 0 },
    { label: 'Categories', cell: (row) => <span className="capitalize">{(row.categories ?? []).join(', ') || '—'}</span> },
    { label: 'Status', cell: (row) => <span className={row.isActive === false ? 'text-muted-foreground' : 'font-medium text-primary'}>{row.isActive === false ? 'Inactive' : 'Active'}</span> },
    { label: 'Actions', className: 'text-right', cell: (row) => <div className="flex justify-end gap-1"><Button size="icon" variant="ghost" title="View details" onClick={() => void openDetails(row)}><Eye className="h-4 w-4" /></Button><Button size="icon" variant="ghost" title="Edit" onClick={() => setForm(toForm(row))}><Pencil className="h-4 w-4" /></Button><Button size="icon" variant="ghost" title={row.isActive === false ? 'Reactivate' : 'Deactivate'} onClick={() => void toggleActive(row)}><Power className={row.isActive === false ? 'h-4 w-4' : 'h-4 w-4 text-destructive'} /></Button></div> },
  ];
  return <div className="space-y-6"><section className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.16em] text-primary">Administrator</p><h1 className="mt-2 font-serif text-4xl tracking-tight">Campground management</h1><p className="mt-3 text-muted-foreground">Manage all four CampFlow locations from one place.</p></div><Button onClick={() => setForm(blankForm())}><Plus />Add campground</Button></section>{error && <ErrorState title="Campgrounds unavailable" message={error} />}<Card className="overflow-hidden"><DataTableControls search={search} onSearch={setSearch} filter={filter} onFilter={setFilter} filterOptions={[{ value: 'all', label: 'All locations' }, { value: 'active', label: 'Active' }, { value: 'inactive', label: 'Inactive' }]} placeholder="Search campgrounds…" />{loading ? <div className="grid min-h-64 place-items-center"><Spinner className="size-6 text-primary" /></div> : data.rows.length ? <><DataTable columns={columns} rows={data.rows} rowKey={idOf} onSort={(key) => { setOrder(key === sort && order === 'asc' ? 'desc' : 'asc'); setSort(key); }} /><DataTablePagination page={data.page} totalPages={data.totalPages} onPageChange={load} /></> : <CardContent className="p-10 text-center text-sm text-muted-foreground">No campgrounds match these controls.</CardContent>}</Card><CampgroundDialog form={form} setForm={setForm} saving={saving} onSave={save} /><Dialog open={!!details} onOpenChange={(open) => !open && setDetails(null)}><DialogContent className="max-h-[85vh] overflow-y-auto"><DialogHeader><DialogTitle>{details?.name}</DialogTitle></DialogHeader>{details && <div className="space-y-5 text-sm"><div className="rounded-xl bg-secondary/45 p-4"><p className="font-medium">{details.shortDescription || details.description}</p><p className="mt-2 text-muted-foreground">{[details.address?.street, details.address?.city, details.address?.state, details.address?.zip].filter(Boolean).join(', ')}</p></div><div className="grid gap-4 sm:grid-cols-2"><Detail label="Contact" value={`${details.email} · ${details.phone}`} /><Detail label="Location" value={`${details.location?.coordinates?.[1] ?? '—'}, ${details.location?.coordinates?.[0] ?? '—'}`} /><Detail label="Categories" value={(details.categories ?? []).join(', ') || '—'} /><Detail label="Amenities" value={(details.amenities ?? []).join(', ') || '—'} /></div></div>}</DialogContent></Dialog></div>;
}

function Detail({ label, value }: { label: string; value: string }) { return <div><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">{label}</p><p className="mt-1 break-words">{value}</p></div>; }
function CampgroundDialog({ form, setForm, saving, onSave }: { form: CampgroundForm | null; setForm: (form: CampgroundForm | null) => void; saving: boolean; onSave: (event: React.FormEvent) => void }) {
  const update = (field: keyof CampgroundForm, value: string | boolean) => form && setForm({ ...form, [field]: value });
  return <Dialog open={!!form} onOpenChange={(open) => !open && setForm(null)}><DialogContent className="max-h-[88vh] max-w-3xl overflow-y-auto"><DialogHeader><DialogTitle>{form?.id ? 'Edit campground' : 'Add campground'}</DialogTitle></DialogHeader>{form && <form className="space-y-5" onSubmit={onSave}><div className="grid gap-4 sm:grid-cols-2"><Field label="Name" value={form.name} onChange={(value) => update('name', value)} required /><Field label="Email" type="email" value={form.email} onChange={(value) => update('email', value)} required /><Field label="Phone" value={form.phone} onChange={(value) => update('phone', value)} required /><Field label="Website" type="url" value={form.website} onChange={(value) => update('website', value)} /><Field label="Street" value={form.street} onChange={(value) => update('street', value)} required /><Field label="City" value={form.city} onChange={(value) => update('city', value)} required /><Field label="State" value={form.state} onChange={(value) => update('state', value)} required /><Field label="Postal code" value={form.zip} onChange={(value) => update('zip', value)} required /><Field label="Country" value={form.country} onChange={(value) => update('country', value)} required /><Field label="Total sites" type="number" min="0" value={form.totalSites} onChange={(value) => update('totalSites', value)} required /><Field label="Longitude" type="number" step="any" value={form.longitude} onChange={(value) => update('longitude', value)} required /><Field label="Latitude" type="number" step="any" value={form.latitude} onChange={(value) => update('latitude', value)} required /><Field label="Categories (comma-separated)" value={form.categories} onChange={(value) => update('categories', value)} required /><Field label="Amenities (comma-separated)" value={form.amenities} onChange={(value) => update('amenities', value)} /></div><div className="space-y-2"><Label htmlFor="camp-description">Description</Label><Textarea id="camp-description" value={form.description} onChange={(event) => update('description', event.target.value)} required /></div><div className="space-y-2"><Label htmlFor="camp-short-description">Short description</Label><Input id="camp-short-description" value={form.shortDescription} onChange={(event) => update('shortDescription', event.target.value)} /></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label>Pet policy</Label><Select value={form.petPolicy} onValueChange={(value) => update('petPolicy', value)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="allowed">Allowed</SelectItem><SelectItem value="restricted">Restricted</SelectItem><SelectItem value="not_allowed">Not allowed</SelectItem></SelectContent></Select></div><Toggle label="Active location" description="Deactivate to hide this location from booking." checked={form.isActive} onChange={(value) => update('isActive', value)} /><Toggle label="Featured" description="Feature this location in customer-facing listings." checked={form.isFeatured} onChange={(value) => update('isFeatured', value)} /></div><Button className="w-full" disabled={saving}>{saving ? <><Spinner />Saving…</> : 'Save campground'}</Button></form>}</DialogContent></Dialog>;
}
function Field({ label, value, onChange, ...props }: { label: string; value: string; onChange: (value: string) => void } & Omit<React.ComponentProps<typeof Input>, 'value' | 'onChange'>) { const id = `field-${label.replace(/\W/g, '-')}`; return <div className="space-y-2"><Label htmlFor={id}>{label}</Label><Input id={id} value={value} onChange={(event) => onChange(event.target.value)} {...props} /></div>; }
function Toggle({ label, description, checked, onChange }: { label: string; description: string; checked: boolean; onChange: (value: boolean) => void }) { return <div className="flex items-center justify-between rounded-lg border p-3"><div><p className="text-sm font-medium">{label}</p><p className="text-xs text-muted-foreground">{description}</p></div><Switch checked={checked} onCheckedChange={onChange} /></div>; }
